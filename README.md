A collection of Docker image files and their tests.
These images are related by FROM inheritance and
share a common testing framework for shared_example.
See individual build context directories for more
specific READMEs.